<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" crossorigin="anonymous">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Pattaya|Roboto">

        {% stylesheets 'assets/css/*' filter='cssrewrite' %}
            <link rel="stylesheet" href="{{ asset_url }}">
        {% endstylesheets %}

        {% if content is defined %}
            {% set title = ez_content_name(content) %}
        {% endif %}
        <title>{{ title|default('Home'|trans) }} - {{ "It's a Dog's World!"|trans }}</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron text-right">
                <div>{{ "It's a Dog's World"|trans }}</div>
            </div>
            <div class="row navbar navbar-default" id="main-navbar">
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        {{ render_esi(controller('app.controller.menu:getMenuItemsAction', {
                            'template': 'pagelayout_menu.html.twig'
                        })) }}
                    </ul>
                </div>
            </div>
            <div class="row main">
                {% block content %}{% endblock %}
            </div>
        </div>

        <footer class="container text-muted text-center">
            <div>
                <small>{{ 'This is a work of fiction. Any resemblance to actual dogs or ibexes, or actual events is purely coincidental'|trans }}</small>
            </div>
        </footer>
    </body>
</html>
